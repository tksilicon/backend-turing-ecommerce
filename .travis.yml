language: java

before_install:
   - gem install bundler

script:
   mvn verify -B


#deploy:
  #provider: heroku
  #api_key: 
    #secure: $HEROKU_API_KEY
  #app: backend-turing-ecommerce
  #on:
    #repo: tksilicon/backend-turing-ecommerce